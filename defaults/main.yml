# Default values for variables

---

# Enable or disable tasks
fedora_upgrade_packages: true
fedora_configure_hostname: true
fedora_configure_timezone: true
fedora_configure_packages: true
fedora_configure_services: true
fedora_configure_kernel_modules: true
fedora_configure_smartd: true
fedora_configure_firewalld: true

fedora_hostname: ''
fedora_timezone: ''

# Packages
fedora_packages: []
# # Individual package
# - name: neovim
#   state: present
# # List of packages
# - name:
#     - ncdu
#     - htop
#   state: present
# - name: rhythmbox
#   state: absent

# Systemd services
fedora_services: []
# - name: firewalld
#   Optional, defaults to true
#   state: started
#
#   Optional, defaults to true
#   enabled: true

# Kernel modules to enable
fedora_kernel_modules: []
# - name: i2c-dev
#   state: present

# S.M.A.R.T. options added to smartd.conf
fedora_smartd_options: []
# - option: DEVICESCAN -a -o on -S on
#   state: present

# Named services to allow through firewalld
fedora_firewalld_services: []
# - name: ssh
#   Optional, defaults to true
#   state: enabled
#
#   # Optional, defaults to true
#   immediate: true
#
#   # Optional, defaults to true
#   permanent: true

# Ports to allow through firewalld
fedora_firewalld_ports: []
# - port: 80/tcp
#   Optional, defaults to true
#   state: enabled
#
#   # Optional, defaults to true
#   immediate: true
#
#   # Optional, defaults to true
#   permanent: true
